import Styles from 'components/PhoneOverlay/PhoneOverlay.module.scss';

const PhoneOverlay = () => {
  return (
    <div className={Styles.wrapper}>
      <p>We're sorry, for now the game works only on wider screens </p>
    </div>
  );
};

export default PhoneOverlay;
