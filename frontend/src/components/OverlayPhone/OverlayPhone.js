// styles
import styles from './OverlayPhone.module.scss';

const OverlayPhone = () => (
  <div className={styles.wrapper}>
    <p>We're sorry, for now the game works only on wider screens </p>
  </div>
);

export default OverlayPhone;
